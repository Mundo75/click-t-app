import React from "react";

const Header = props => (
    <div className="container text-center" id="header-container">
        <h1 id="title"><b>Click "T" Game</b></h1>
    </div>
);

export default Header;
